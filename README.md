# (Go)host

## What is this?

This is a DNS server written in Go that forwards requests to an upstream DNS server.

## How to run it?

```bash
go run main.go
```

## How to test it?

```bash
dig @127.0.0.1 pkdiv.com
```